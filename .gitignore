# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
.venv/
venv/
ENV/
env/

# Jupyter Notebook
.ipynb_checkpoints

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
*.log
.env

# ML models (binaires non commités — constitution Principe IV)
ml_models/saved_models/*.pkl
data/big_data/

# MLflow (tracking/registry — artefacts générés localement, non versionnés)
mlruns/
mlflow.db

# Backups ETL (données externes brutes — non versionnées)
data/external/*
# Exception : BDD externe de référence (4 lignes statiques, source ETL
# committée au même titre que insurance.csv — pas un backup généré)
!data/external/stats_regionales.db

.claude/

# Specs (documentation interne, non versionnée)
specs/
.specify/
CLAUDE.md