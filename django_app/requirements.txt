# Django — Interface web principale AssuML (port 8001)
# Versions figées à l'identique de l'image Docker validée en production :
# une reconstruction ultérieure installe exactement les mêmes bibliothèques.
django==5.2.17
gunicorn==26.1.0
whitenoise==6.12.0

# HTTP vers FastAPI
requests==2.34.2

# PostgreSQL
psycopg2-binary==2.9.12

# Variables d'environnement
python-dotenv==1.2.3

# Big Data (Analytics — import direct DuckDB, exception à la règle FastAPI)
duckdb==1.5.5
pyarrow==24.0.0
pandas==2.3.3

# Tests
pytest-django==4.14.0
pytest==8.4.2

# Qualité
flake8==7.3.0
black==25.11.0
